<footer class="footer">
    <div class="container">
        <div class="footer-content">
            <div class="footer-section">
                <h5 class="footer-heading">LocalEvent</h5>
                <p class="footer-text">Discover and join amazing events happening in your community. Create your own events and connect with like-minded people.</p>
            </div>
            
            <div class="footer-section">
                <h5 class="footer-heading">Quick Links</h5>
                <ul class="footer-links">
                    <li><a href="{{ path('app_home') }}">Home</a></li>
                    <li><a href="{{ path('event_index') }}">Events</a></li>
                    <li><a href="{{ path('app_about') }}">About Us</a></li>
                </ul>
            </div>
            
            <div class="footer-section">
                <h5 class="footer-heading">Account</h5>
                <ul class="footer-links">
                    {% if app.user %}
                        <li><a href="{{ path('app_dashboard') }}">My Dashboard</a></li>
                        <li><a href="{{ path('app_profile') }}">Profile</a></li>
                        <li><a href="{{ path('event_new') }}">Create Event</a></li>
                    {% else %}
                        <li><a href="{{ path('app_login') }}">Log In</a></li>
                        <li><a href="{{ path('app_register') }}">Sign Up</a></li>
                    {% endif %}
                </ul>
            </div>
            
            <div class="footer-section">
                <h5 class="footer-heading">Connect With Us</h5>
                <div class="social-links">
                    <a href="#" class="social-link"><i class="bi bi-facebook"></i></a>
                    <a href="#" class="social-link"><i class="bi bi-twitter-x"></i></a>
                    <a href="#" class="social-link"><i class="bi bi-instagram"></i></a>
                    <a href="#" class="social-link"><i class="bi bi-linkedin"></i></a>
                </div>
            </div>
        </div>
        
        <div class="footer-bottom">
            <p>&copy; {{ 'now'|date('Y') }} LocalEvent. All rights reserved.</p>
            <p>Made with <i class="bi bi-heart-fill text-danger"></i> and Symfony</p>
        </div>
    </div>
</footer>

<style>
.footer {
    background-color: var(--neutral-900);
    color: var(--neutral-300);
    padding: 4rem 0 2rem;
}

.footer-content {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
    margin-bottom: 3rem;
}

.footer-heading {
    color: white;
    font-size: var(--fs-lg);
    font-weight: 600;
    margin-bottom: 1.5rem;
}

.footer-text {
    font-size: var(--fs-sm);
    line-height: 1.6;
}

.footer-links {
    list-style: none;
    padding: 0;
    margin: 0;
}

.footer-links li {
    margin-bottom: 0.75rem;
}

.footer-links a {
    color: var(--neutral-300);
    text-decoration: none;
    transition: color 0.2s ease;
    font-size: var(--fs-sm);
}

.footer-links a:hover {
    color: white;
}

.social-links {
    display: flex;
    gap: 1rem;
}

.social-link {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 50%;
    background-color: rgba(255, 255, 255, 0.1);
    color: white;
    font-size: var(--fs-lg);
    transition: all 0.3s ease;
}

.social-link:hover {
    background-color: var(--primary-500);
    transform: translateY(-3px);
}

.footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 2rem;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    font-size: var(--fs-xs);
}

@media (max-width: 768px) {
    .footer-bottom {
        flex-direction: column;
        gap: 1rem;
        text-align: center;
    }
}
</style>
